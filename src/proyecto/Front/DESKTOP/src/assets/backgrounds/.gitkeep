# This directory is used at runtime to store uploaded floorplan background templates.

The application will save user-selected background images here when changing the table plan template.  It’s left empty at first, with this `.gitkeep` file ensuring the directory exists in version control.